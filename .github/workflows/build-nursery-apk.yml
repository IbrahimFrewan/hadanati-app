name: Build Nursery Android APK

on:
  push:
    branches: [main]
    paths:
      - 'NurseryApp/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: NurseryApp/package-lock.json

      - name: Install dependencies
        working-directory: NurseryApp
        run: npm install

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Verify token
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas whoami

      - name: Initialize EAS project
        working-directory: NurseryApp
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas init --non-interactive --force || true

      - name: Build APK
        working-directory: NurseryApp
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas build --platform android --profile preview --non-interactive --no-wait

      - name: Print build URL
        working-directory: NurseryApp
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          echo "✅ Build submitted! Track progress and download APK at:"
          eas build:list --platform android --limit 1 --json --non-interactive | jq -r '.[0] | "Status: \(.status)\nURL: \(.artifacts.buildUrl // "Building... check https://expo.dev/builds")"'
